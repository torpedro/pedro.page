# pedro.page

## Prerequisites

 * Yarn

## JS Dependencies

Run `yarn install` to install dependencies listed in `package.json`.

Dependencies are

 * TypeScript
 * React + ReactDOM (+ typings)
 * Parcel

## Local Test

`yarn run start`

## Deploy

`yarn run build`
